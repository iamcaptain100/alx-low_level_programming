#include <stdio.h>

/**
* main - A program that print a line using the printf funnction 
* Return: 0 when done
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
